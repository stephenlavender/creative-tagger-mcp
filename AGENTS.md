# Agent instructions

This repository is public: competitor-data examples and fixtures (Meta Ad Library
page names, ad copy, spend figures) must use obviously synthetic brands — e.g.
"Everwell Labs (Demo)" — never real brand names or real `ads_archive` rows.
